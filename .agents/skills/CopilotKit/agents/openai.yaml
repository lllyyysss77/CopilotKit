interface:
  display_name: "CopilotKit"
  short_description: "Repo-specific patterns and workflows for CopilotKit"
  default_prompt: "Use the CopilotKit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true